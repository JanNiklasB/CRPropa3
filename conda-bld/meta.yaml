package:
  name: crpropa
  version: {{ environ.get('GITHUB_REF_NAME') }}

source:
  git_url: ../
  folder: crpropa

build:
  script_env:
    - FAST_WAVES=OFF
    - SIMD_EXTENSIONS=none

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('fortran') }}
    - git
    - cmake
    - ninja
    - swig
  
  host:
    # host only requirements
    - zlib
    - gperftools
    - pybind11-stubgen
    # crpropa requirements
    - fftw
    - hdf5
    - muparser
    - eigen
    # python requirements
    - python
    - numpy >=1.26,<3

  run:
    # crpropa requirements
    - fftw
    - hdf5
    - muparser
    - eigen
    # cmake required for tests
    - cmake
    # python requirements
    - python
    - numpy >=1.26,<3

test:
  imports:
    - crpropa
  commands:
    - testCRPropa